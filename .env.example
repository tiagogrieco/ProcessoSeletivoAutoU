# OpenAI API Key (opcional)
# Se você possui uma chave da OpenAI, cole aqui para usar classificação com AI.
# Caso contrário, o sistema usará um modo offline baseado em palavras-chave.
OPENAI_API_KEY=sua-chave-api-aqui

# Porta do servidor (opcional, padrão: 5000)
PORT=5000
